- 插件以常量 `Name` 暴露 OTel 对齐的名称，并与 workDir 下的目录键保持一致。
- 通过 `New(binDir, workDir, log)` 构造器返回 `plugins.Plugin`，binDir/workDir/log 全部可注入以便测试。
- 从 `cfg.Spec` 读取 string 或 []string 时使用统一的 `stringSpec`/`stringSliceSpec` 辅助函数做类型断言与默认值回退。
- 子进程启动前检查二进制存在、创建 workDir、将 stdout/stderr 追加到 `<workDir>/<id>.log`，并通过 `cmd.Cancel` 发送 SIGTERM + `WaitDelay` 优雅退出。
- 健康状态更新集中在 `setPluginState`/`setSourceState` 等带互斥锁的私有方法中，panic 恢复后一律调用并将错误写入 `LastError`。