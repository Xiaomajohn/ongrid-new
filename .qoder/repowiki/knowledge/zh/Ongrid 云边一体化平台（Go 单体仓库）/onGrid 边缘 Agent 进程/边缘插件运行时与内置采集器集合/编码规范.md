- 插件以 OTel 信号复数命名（"metrics"/"logs"/"traces"），作为 `Name()` 返回值兼注册键
- 子进程型插件继承 `SubprocessPlugin`，通过 SIGHUP 热重载或 stop+start 冷重启响应配置变更
- `PluginConfig.Spec` 使用 `map[string]interface{}` 承载插件特定 JSON，由各自 `spec.go`/`render.go` 解码渲染
- 健康状态通过 `HealthSnapshot()` 返回 `PluginHealth`，多目标指标插件额外填充 `Targets` 数组