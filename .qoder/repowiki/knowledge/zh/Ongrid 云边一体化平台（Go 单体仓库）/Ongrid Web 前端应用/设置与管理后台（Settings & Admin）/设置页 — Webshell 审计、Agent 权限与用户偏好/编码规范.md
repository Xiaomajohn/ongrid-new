- 所有用户可见文案使用 `tr(zh, en)` 双语文案包裹，按钮/提示/占位符无一例外。
- UI 组合类名统一通过 `cn(...)` 合并，避免字符串拼接。
- 异步副作用用 `useCallback` + `useEffect` 触发，并在 try/catch 中把错误落回本地 `err/error` state，而非抛给上层。
- 写操作采用乐观更新：先设本地 state，再发请求，失败时回滚到旧值。