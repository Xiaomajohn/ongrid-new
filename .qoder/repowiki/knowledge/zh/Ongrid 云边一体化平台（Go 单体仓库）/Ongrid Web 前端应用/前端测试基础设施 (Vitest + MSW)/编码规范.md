- 所有 MSW 请求拦截在单个 `*.test.tsx` 内通过 `server.use(...)` 就近注册，禁止在 setup 或 fixtures 中定义路由，使 fixture 与断言一一对应。
- 共享数据对象放在 `src/test/fixtures/` 下并按领域分文件，类型直接来自 `@/api/*` 的 wire shape，避免在测试中手写 JSON。
- 测试生命周期钩子集中写在 `setup.ts`，对外只暴露 `server` 实例，测试文件不直接调用 `setupServer`。