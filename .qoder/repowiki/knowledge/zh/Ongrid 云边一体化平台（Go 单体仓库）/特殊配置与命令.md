根 Makefile 是唯一构建入口：`make build` 同时编译 ongrid 与 ongrid-edge；`make package` 串联 fetch-* 外部二进制、docker-build、edge bundle 与 dist/package.sh 输出 tarball；`make patch` 基于 git diff 打增量补丁包；`make compose-up` 拉起本地开发栈。