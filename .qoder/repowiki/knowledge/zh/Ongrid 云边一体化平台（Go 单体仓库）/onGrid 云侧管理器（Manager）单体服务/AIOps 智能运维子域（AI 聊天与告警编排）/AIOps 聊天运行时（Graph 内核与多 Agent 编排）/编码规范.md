- 对外返回的切片一律做值拷贝再返回（All/Warnings/Resolve/snapshotWorker），避免调用方持有对内部可变状态的引用。
- YAML/JSON 标签统一采用 snake_case，使同一份 SKILL.md / agent .md 能同时被 ongrid、claude-code、openclaw 消费。
- 可选依赖通过 post-construction setter 注入（SetMentionResolver / SetCredentialBinder / SetAgentWriteEnabledProvider / SetToolBag），构造时只校验必填项（Sessions、ChatModel）。
- 错误处理遵循“非致命警告走 LoadWarning 列表、致命错误直接返回”的分流：walk 失败、parse 失败、container 加载失败都记录 Warning 而非中断整个目录扫描。
- 上下文透传通过 basetool.WithXxx / XxxFromContext 成对进行（Locale、LLMChoice、FilteredTools、SessionID、HostWriteAllowed、BoundCredentials、ArtifactSource），不在 Request 中显式传递。