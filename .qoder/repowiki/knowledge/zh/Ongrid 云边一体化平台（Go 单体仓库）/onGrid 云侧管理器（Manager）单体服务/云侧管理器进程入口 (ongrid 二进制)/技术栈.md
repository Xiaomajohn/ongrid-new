Go 单二进制；HTTP 路由基于 `github.com/go-chi/chi/v5`；遥测使用 `go.opentelemetry.io/contrib/instrumentation/net/http/otelhttp` 上报 Tempo OTLP HTTP；数据库迁移统一走 GORM 风格的 `Migrate(db)` 函数；JWT 签名由 `internal/pkg/auth` 提供；与边端通信通过自研 `frontierbound` SDK 长连接。