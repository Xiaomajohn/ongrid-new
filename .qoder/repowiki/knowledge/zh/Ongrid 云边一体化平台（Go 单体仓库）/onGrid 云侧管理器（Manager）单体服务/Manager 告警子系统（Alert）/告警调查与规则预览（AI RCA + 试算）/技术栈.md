基于 eino chatruntime 的 ReAct agent（agent name `incident-investigator`）作为调查工作器；结构化提取使用独立的 `llm.MultiClient`（可配置 model/provider/timeout），失败回退到首段启发式；PromQL 通过 `promquery.Client.QueryRange` 矩阵查询，LogQL 通过 `logquery.Client` 范围查询。