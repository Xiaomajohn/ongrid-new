Go + chi/v5 HTTP 路由；GORM 持久化 MySQL；qdrantx 客户端对接 Qdrant 向量库；文本嵌入由 `internal/pkg/embedding` 抽象（默认维度 1536，可通过 `ONGRID_EMBEDDING_DIM` 覆盖）；PDF/DOCX 提取走 `internal/pkg/docextract`；git 操作通过 `os/exec` 调用系统 git，HTTPS PAT 经 `GIT_ASKPASS` 环境变量注入，避免进入进程命令行。