HTTP 路由基于 `github.com/go-chi/chi/v5`；鉴权上下文统一从 `internal/pkg/tenantctx` 读取租户角色；错误体系使用 `internal/pkg/errs` 的 sentinel（ErrInvalid / ErrUnauthorized / ErrForbidden / ErrNotFound / ErrNotWiredYet），并通过 `errs.HTTPStatus` 映射到 HTTP 状态码。